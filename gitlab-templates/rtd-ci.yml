---
build:
  stage: docs
  image: python:3
  script:
    - pip install sphinx
    - pip install sphinx-rtd-theme
    - pip install recommonmark
    - cd doc ; make html
    - mv build/html/ ../public/
