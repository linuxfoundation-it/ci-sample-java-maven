---
image: maven:3-jdk-13

cache:
  paths:
    - .m2/repository/

build:
  stage: build
  script: mvn $MAVEN_CLI_OPTS clean verify

publish:
  stage: publish
  script: mvn $MAVEN_CLI_OPTS deploy
  only:
    - master
